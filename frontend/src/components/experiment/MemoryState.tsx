import type { TraceItem } from '../../types/experiment';
export function MemoryState({state,trace}:{state:number[];trace:TraceItem|null}) { return <div className="flex flex-col gap-step-xs bg-surface-container-low p-step-md rounded-DEFAULT"><div className="flex items-center justify-between"><span className="font-label-data-sm text-label-data-sm text-primary font-semibold uppercase tracking-wider">Fixed-Size Recurrent State Matrix (K = {state.length} Slots)</span><span className="font-caption-italic text-caption-italic text-on-surface-variant">Values originate from the backend memory trace</span></div><div className="grid grid-cols-2 sm:grid-cols-4 md:grid-cols-8 gap-step-sm">{state.map((value,id)=>{const active=trace?.updated_slot===id;return <div key={id} className={`p-step-sm rounded-DEFAULT shadow-sm flex flex-col justify-between h-24 ${active?(trace?.collision?'bg-error-container text-on-error-container':'bg-secondary-container text-on-secondary-container'):'bg-surface-container-lowest text-primary'}`}><span className="font-bold text-[11px]">SLOT {String(id).padStart(2,'0')}</span><span className="font-label-data-lg font-bold text-center">{value.toFixed(3)}</span><span className="text-[10px] opacity-80">{active?'ACTIVE WRITE':'state value'}</span></div>})}</div><span className="font-label-data-sm text-label-data-sm text-on-surface-variant">{trace?`Writes: ${trace.active_slots} active slots · Collisions: ${trace.collision_count} · Target signal: ${trace.target_signal?.toFixed(3)??'—'}`:'Reset state shown before token ingestion.'}</span></div> }
