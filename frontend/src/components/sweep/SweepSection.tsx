import { useEffect,useState } from 'react';import { runSweep } from '../../api/experiments';import type { SweepResponse } from '../../types/experiment';
export function SweepSection(){const [data,setData]=useState<SweepResponse|null>(null);const [error,setError]=useState<string|null>(null);useEffect(()=>{runSweep({sweep_parameter:'memory_slots',sweep_values:[2,4,8,16,32],base_sequence_length:32,base_memory_slots:8,base_vocabulary_size:20,base_target_token:7,base_interference:0,seed:42}).then(setData).catch(e=>setError(e instanceof Error?e.message:'Sweep unavailable.'));},[]);return <section className="max-w-ledger-width mx-auto px-step-lg py-step-xl w-full" id="sweep-analysis"><div className="bg-surface-container-lowest p-step-lg rounded-DEFAULT shadow-sm"><span className="text-secondary font-semibold">SECTION 07 & 08 • PARAMETRIC SWEEP</span><h2 className="font-headline-lg text-primary">Capacity vs. Recall Degradation Curve</h2><p>This sweep is calculated by the backend toy engine; it is not a general scientific claim.</p>{error?<p role="alert" className="text-error">{error}</p>:!data?<p role="status">Running backend sweep…</p>:<div className="overflow-x-auto"><table className="w-full text-left"><thead><tr><th>Memory slots</th><th>Accuracy</th><th>Collision rate</th><th>Utilization</th><th>Target signal</th></tr></thead><tbody>{data.results.map(r=><tr key={r.experiment_id}><td>{r.parameter}</td><td>{(r.accuracy*100).toFixed(1)}%</td><td>{(r.collision_rate*100).toFixed(1)}%</td><td>{(r.memory_utilization*100).toFixed(1)}%</td><td>{r.target_signal.toFixed(3)}</td></tr>)}</tbody></table></div>}</div></section>}
